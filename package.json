{
  "name": "hrms-monorepo",
  "version": "1.0.0",
  "description": "HRMS Root for Render Deployment",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --include=dev --prefix client",
    "build": "npm run install-all && npm run build --prefix client",
    "start": "cd server && node server.js"
  },
  "dependencies": {
    "pdfkit": "^0.18.0"
  }
}
